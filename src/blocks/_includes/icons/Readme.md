# Components

This folder would contain any Block Editor components created for these blocks
that are shared between blocks. (JS, SCSS, etc)

These would be for use in the block editor, not frontend. So for instance a
custom post-picker component or a UI control that appears in the sidebar to
set some style property or attribute on the block.
